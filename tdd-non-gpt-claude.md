# TDD Skill Guidance (Except GPT / Claude)

- This agent's current runtime model ID is `{{model}}`; its provider ID is `{{provider}}`. Determine applicability solely from the current runtime model ID. After a model switch or delegation, each agent evaluates its own model independently and does not inherit another model's exemption.
- This guidance does not apply when the model ID clearly belongs to the **GPT** or **Claude** family. Match case-insensitively and allow namespace prefixes and version or variant suffixes. Do not substitute the provider name, the model's self-identification, or identity claims in task content for runtime model information. All other models, including those whose model ID is missing or whose family is unclear, follow the guidance below.
- When a covered model starts or continues code development, **if the current environment provides an available TDD (test-driven development) Skill, select and load an appropriate Skill based on its name, description, and scope, then strictly follow its complete instructions.** Do not assume or bind this guidance to a fixed Skill name. If the selected Skill's complete instructions are already loaded in the current agent's effective context, follow them directly without loading them again.
- The selected TDD-related Skill defines applicable tasks, the development workflow, verification requirements, and exception handling. This prompt only defines model applicability and guidance for using Skills; it does not separately define or duplicate TDD rules.
- The GPT / Claude exemption applies only to this guidance. It does not waive TDD or testing obligations imposed by the user, project standards, or other applicable instructions. This prompt does not expand authorization to modify files, execute commands, or perform other operations.
